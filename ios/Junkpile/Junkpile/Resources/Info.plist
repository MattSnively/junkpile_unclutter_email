<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Bundle Identifier -->
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>

	<!-- Bundle Name -->
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>

	<!-- Version Numbers -->
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>

	<!-- App Category -->
	<key>LSApplicationCategoryType</key>
	<string>public.app-category.productivity</string>

	<!-- URL Schemes for OAuth Callback -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLName</key>
			<string>com.junkpile.app</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<!-- Custom URL scheme for OAuth redirect -->
				<string>com.junkpile.app</string>
				<!-- Google Sign-In reversed client ID (matches AppConfig.googleReversedClientID) -->
				<string>com.googleusercontent.apps.758350179240-rlbdcla4brd4ffus0ln3nvfma5pn9j5r</string>
			</array>
		</dict>
	</array>

	<!-- Required Device Capabilities -->
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>

	<!-- Supported Interface Orientations -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>

	<!-- App Transport Security -->
	<!-- No exceptions needed: Release builds use HTTPS via AppConfig.apiBaseURL. -->
	<!-- For local development (Debug builds), add NSAllowsLocalNetworking in -->
	<!-- your Debug.xcconfig or Xcode build settings if needed for simulator. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<!-- Allow local networking (localhost/127.0.0.1) in all builds.        -->
		<!-- This is safe for App Store review — it only affects on-device      -->
		<!-- loopback connections, not arbitrary HTTP. Apple allows this.         -->
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>

	<!-- Export Compliance -->
	<!-- The app implements no encryption of its own: no CryptoKit, no          -->
	<!-- CommonCrypto, no bundled crypto libraries. It only uses what the OS    -->
	<!-- provides — TLS via URLSession and Keychain for token storage — which   -->
	<!-- is exempt. Declaring this here stops App Store Connect asking the      -->
	<!-- encryption questions on every submission. Revisit if the app ever      -->
	<!-- implements its own algorithm; the backend's AES-256-GCM is server-side -->
	<!-- and does not apply to the distributed binary.                          -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>

	<!-- Privacy Descriptions -->
	<key>NSUserTrackingUsageDescription</key>
	<string>We do not track you. This permission is only requested if required by ad networks.</string>

	<!-- Launch Screen -->
	<key>UILaunchScreen</key>
	<dict>
		<key>UIColorName</key>
		<string>LaunchScreenBackground</string>
	</dict>

	<!-- Status Bar Style -->
	<key>UIStatusBarStyle</key>
	<string>UIStatusBarStyleDefault</string>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>

	<!-- Background Modes (for future notification support) -->
	<key>UIBackgroundModes</key>
	<array>
		<string>fetch</string>
		<string>remote-notification</string>
	</array>
</dict>
</plist>

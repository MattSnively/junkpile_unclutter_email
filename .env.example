# Gmail OAuth Credentials
# Get these from: https://console.cloud.google.com/apis/credentials
GMAIL_CLIENT_ID=your_client_id_here
GMAIL_CLIENT_SECRET=your_client_secret_here
GMAIL_REDIRECT_URI=http://localhost:3000/auth/google/callback

# Session Secret
SESSION_SECRET=your_random_session_secret_here

# Apple Sign In
# Bundle ID must match the iOS app's bundle identifier
APPLE_BUNDLE_ID=com.junkpile.app

# JWT Secret for server-issued session tokens (Apple Sign-In users)
# Generate a strong random string for production
JWT_SECRET=your_random_jwt_secret_here

# ============================================================
# RAILWAY DEPLOYMENT NOTES
# ============================================================
# Set these environment variables in the Railway dashboard
# (Settings > Variables) — do NOT commit real values to git.
#
# Required for Railway:
#   NODE_ENV=production
#   GMAIL_CLIENT_ID       — from Google Cloud Console
#   GMAIL_CLIENT_SECRET    — from Google Cloud Console
#   GMAIL_REDIRECT_URI     — https://{your-railway-url}/auth/google/callback
#   SESSION_SECRET         — generate: openssl rand -hex 32
#   JWT_SECRET             — generate: openssl rand -hex 32
#   APPLE_BUNDLE_ID        — com.junkpile.app
#
# PORT is injected automatically by Railway — do not set it manually.
